#include <stdio.h>
int main() {
    //float = decimal 
    float gpa = 2.5;
    float price = 19.99;
    float temprature = -26.6;

    printf("your gpa is %f\n", gpa);
    printf("the price is %f\n", price);
    printf(" thetemprature outside is %f\n", temprature);

    return 0;
}
//float only stores 5-6 digits after decimal wherass
//double stores 15-16 digits after decimal 
// if we want to show the no of digits we want then %.nf where n = number of decimas we want to show 
