#include <stdio.h>
 int main(){
    // a variable =  a reausible contaanier for value
    // behaves as if it was the value it contains 

    int age = 18;
    int year = 2025;
    int quantity = 1;

    printf("you are %d years old\n");
    printf("the year is %d\n" , year);
    printf("you have ordered %d x items\n", quantity);

    return 0;
    
 }
 // %d= format specfier 
